<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Builder AI two-pass pipeline
    |--------------------------------------------------------------------------
    |
    | Pass 1 is an ultra-compact classifier for local Ollama/Qwen inference.
    | It returns only {"type":"предложение услуги"} or {"type":"мусор"}.
    | Pass 2 keeps the existing extraction/normalization flow and runs only
    | after a positive Pass 1 decision.
    |
    */

    'two_pass_ollama_enabled' => env('BUILDER_AI_TWO_PASS_OLLAMA_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | Pass 1 compact classifier prompt (fallback)
    |--------------------------------------------------------------------------
    |
    | Can be overridden from MoonShine -> Настройки via
    | `builder_pass1_prompt` (textarea).
    |
    */
    'pass1_prompt' => <<<'PROMPT'
Ты классифицируешь одно сообщение из строительного Telegram-чата.

Ответ строго одним JSON-объектом без markdown, без пояснений и без текста до/после JSON:
{"type":"предложение услуги"} или {"type":"мусор"}
Разрешён только один ключ: type. Никаких других полей.

Главный критерий — кто говорит, а не «строительные слова»:
«предложение услуги» только если автор = исполнитель (я/мы) и сам предлагает сделать работы на объекте руками (ремонт, монтаж, отделка, стройка). Строительная лексика без явной роли исполнителя → мусор.

Решай в таком порядке (первое сработавшее правило — финал):
1) Автор ищет людей, бригаду, мастеров, исполнителей, набирает на смену/объект, «нужна бригада», «ищем бригаду», «бригада из N человек» в найме, «кто свободен», «есть кто на объект», «ищу исполнителя» без самопитча исполнителя → мусор.
2) Автор как работодатель/заказчик: предлагает работу/подработку/смену как слот («есть работа», «руб/смена», «за смену», «выплата в течение … часов», «от … лет», «заработок», «требуются», тендер, «только ИП и ООО», «аванс N%», «пишите»+ссылка в рекрутинговом стиле) → мусор. Слова «работа»/«срочно» считай наймом только в таком контексте; «ищу работу», «срочно выйдем на объект» у исполнителя — не основание для пункта 2.
3) Заказчик: «нужно/надо сделать», короткий заказ без «мы делаем/выполним» → мусор.
4) Проектная/BIM-реклама разделов (ППР, АР, КР, ГСН, ГСВ, «разработаем комплект», «проектирую») без полевых работ как услуги бригады → мусор.
5) Спам: почти нет кириллицы при длинном тексте, стена эмодзи/символов, латинский клавиатурный мусор, подмена похожими буквами разных алфавитов → мусор.
6) Материалы, аренда, продажа, оффтоп, обсуждение без предложения услуг исполнителя → мусор.

Только если ни одно из правил 1–6 не подходит и при этом есть явный самопитч исполнителя — «предложение услуги». Примеры допустимых формулировок: «мы/я делаем, выполняем, оказываем», «возьмём объём/объект», «бригада выполнит …», «мы бригада … монтируем/отделываем», «свободны, ищем заказ/объём» в связке с «делаем/выполним». Одно слово «бригада» или «мастер» без предложения услуг от первого лица — не положительный маркер.
Нужно достаточно кириллицы для связного описания услуги (не одна ссылка и не только хэштеги/цифры).
При любом сомнении — "мусор".

Не извлекай поля. Не нормализуй. Не добавляй ключи кроме type.
PROMPT,

    /*
    |--------------------------------------------------------------------------
    | Pass 2 default prompt (fallback)
    |--------------------------------------------------------------------------
    |
    | Used only when source channel has empty `ai_promt`.
    | Can be overridden from MoonShine -> Настройки via
    | `builder_pass2_default_prompt` (textarea).
    |
    */
    'pass2_default_prompt' => <<<'PROMPT'
Ты анализируешь одно Telegram-сообщение строительного чата.
Верни СТРОГО один JSON-объект без markdown и без пояснений.

Классификация (поле "type" в JSON — это тип объявления; дополнительного ключа ai_type не добавляй):
- "type":"предложение услуги" только если автор сам предлагает выполнять строительные/ремонтные/монтажные/отделочные работы на объекте как исполнитель (мастер/бригада), явно от своего имени («мы делаем», «выполним», «работаем»), и в тексте есть связное русскоязычное описание (не одна ссылка и не «стена» из эмодзи/символов).
- Если почти нет кириллицы, одни смайлы/украшения, латинский клавиатурный мусор, только реклама проектных разделов (ППР, АР, КР, ГСН, BIM и т.п.) без полевых работ — ставь "type":"мусор".
- Если автор ищет работников, предлагает работу/подработку/смену, ищет исполнителя, пишет «требуется …», «требуется подрядчик», тендер, «кто занимается …», набор бригады «из N человек» под объём, «только ИП/ООО», «аванс %», или пишет как заказчик "нужно сделать..." — это не предложение услуги (обычно "вакансия" или "мусор" по смыслу).
- При сомнении выбирай "мусор" или "вакансия", не "предложение услуги".

Не выдумывай факты. Если данных нет — ставь пустую строку, null или [] по типу поля.

Формат ответа (ключи строго такие):
{
  "type": "",
  "reason": "",
  "experience": "",
  "soft_experience": "",
  "education": "",
  "work_schedule": "",
  "total_work_project": "",
  "type_of_work": "",
  "price_by_hour": "",
  "price_by_project": "",
  "price_by_month": "",
  "about": "",
  "spec_requirements": "",
  "link_resume": "",
  "contact_info": [],
  "service_type": "",
  "specialities": [],
  "object_types": [],
  "performer_raw": "",
  "performer_type": "",
  "equipment_skills": [],
  "legal_form": "",
  "location_city": "",
  "location_region": "",
  "price_comment": ""
}
PROMPT,
];
