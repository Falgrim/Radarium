<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Публикация AI-импорта в публичный каталог (Active)
    |--------------------------------------------------------------------------
    |
    | После успешного разбора ИИ записи могут быть либо сразу в каталоге
    | (Active), либо ожидать модерации (InModeration), если не выполнены
    | условия post-filter этого файла.
    |
    */

    'enabled' => env('CATALOG_PUBLICATION_GATE_ENABLED', true),

    /*
    | Требовать хотя бы одну сопоставленную специализацию из словаря
    | перед автоматической публикацией (Active).
    */
    'require_matched_speciality' => env('CATALOG_PUBLICATION_GATE_REQUIRE_SPECIALITY', true),
];
